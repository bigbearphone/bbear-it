
import {store} from './store.js';
export function renderReports(){document.querySelector('#reports').innerHTML=`<div class="pagehead"><div><small>Report Engine</small><h1>รายงาน</h1><p>Preview • Print/PDF • Telegram</p></div></div><section class="card form"><div class="cols3"><label>เริ่ม<input type="date" id="from" value="2026-08-01"></label><label>สิ้นสุด<input type="date" id="to" value="2026-08-06"></label><label>ประเภท<select id="rt"><option>Stock ทุกคลัง</option><option>Finance ค้างรับ</option><option>Partner Commission</option></select></label></div><div class="reportactions"><button class="primary" id="preview">Preview</button><button class="soft" id="print">Print/PDF</button><button class="telegram" id="tg">Telegram</button></div></section><section class="card" id="rr"></section>`;const report=()=>{const t=document.querySelector('#rt').value,d=store();let h=`<h3>${t}</h3><p>${document.querySelector('#from').value} ถึง ${document.querySelector('#to').value}</p>`;if(t==='Stock ทุกคลัง')h+=d.branches.map(b=>`<div class="drillcard"><b>${b.name}</b><small>${d.inventory.filter(x=>x.branch===b.name).map(x=>x.name+' '+x.color).join(', ')||'ไม่มีสินค้า'}</small></div>`).join('');if(t==='Finance ค้างรับ')h+=`<div class="drillcard">PPLAN เหลือ ฿182,500</div><div class="drillcard">Samsung เหลือ ฿191,200</div><div class="drillcard">SG เหลือ ฿76,300</div>`;if(t==='Partner Commission')h+=d.partners.map(p=>`<div class="drillcard"><b>${p.name}</b><small>${p.products.join(', ')} • ค้างจ่าย ฿${p.due.toLocaleString()}</small></div>`).join('');document.querySelector('#rr').innerHTML=h};document.querySelector('#preview').onclick=report;document.querySelector('#print').onclick=()=>window.print();document.querySelector('#tg').onclick=()=>toast('ส่ง Telegram แล้ว');report()}
