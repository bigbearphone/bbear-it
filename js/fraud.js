
const cases=[['แก้ราคาขายต่ำกว่ากำหนด','BLOCKED: ต้องกรรมการอนุมัติ'],['เปลี่ยน IMEI หลังจอง','BLOCKED: Identifier ถูกล็อก'],['รับเงินแต่ไม่ออกบิล','BLOCKED: ปิดยอดไม่ได้'],['ย้ายของไม่มีผู้รับ','BLOCKED: คงสถานะระหว่างขนส่ง'],['เปลี่ยน Partner หลังส่งมอบ','BLOCKED: ต้อง Cancel/Approval'],['ลบ Sale','BLOCKED: ใช้ Cancel Transaction เท่านั้น']];
export function renderFraud(){document.querySelector('#fraud').innerHTML=`<div class="pagehead"><div><small>Fraud Prevention</small><h1>Fraud Test</h1><p>จำลองช่องโหว่</p></div></div><div class="fraudgrid">${cases.map((x,i)=>`<article class="fraudcard"><h3>FRAUD-${String(i+1).padStart(3,'0')}</h3><p>${x[0]}</p><button class="soft" data-fraud="${i}">จำลอง</button><div id="fr${i}" class="notice">ยังไม่ทดสอบ</div></article>`).join('')}</div>`;document.querySelectorAll('[data-fraud]').forEach(b=>b.onclick=()=>{document.querySelector('#fr'+b.dataset.fraud).textContent=cases[b.dataset.fraud][1];toast('ระบบบล็อกเหตุการณ์แล้ว')})}
